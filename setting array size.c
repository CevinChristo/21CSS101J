#include <stdio.h>

int main() {
  // Declare an array of four integers:
  int myNumbers[4];

  // Add elements to it
  myNumbers[0] = 10;
  myNumbers[1] = 20;
  myNumbers[2] = 30;
  myNumbers[3] = 40;

  printf("%d\n", myNumbers[0]);
 
  return 0;
}
