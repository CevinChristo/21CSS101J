#include <stdio.h>
int main() {
  printf("RA2211042010002\n\n");
  int count, n;
  float x, y;
  printf("Enter the values of x and n : ");
  scanf("%f %d", &x, &n);
  y = 1.0;
  count = 1;
  while (count<=) {
    y = y*x;
    count++;
  }
 printf("\nx=%f; n= %d; x to the power n = %f\n",x,n,y);
}
    
    
      
