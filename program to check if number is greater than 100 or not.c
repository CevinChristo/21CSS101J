#include <stdio.h>
int main() {
int number;
printf("Enter an integer number\n\n");
scanf("%d",&number);
if(number<100) {
printf("Number is smaller than 100\n); }
else if (number=100); {
printf("Number is 100); }
else {
printf("Number contains more than two digits and is greater than 100);
}
}
