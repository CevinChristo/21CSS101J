#include <stdio.h>

int main() {
 printf("RA2211042010002\n\n");
  int x = 6;
  int y = 3;
   // shows 1 because 6 is greater than 3 but 6 is not less than 4.
  printf("%d", x > 3 || x < 4);
  return 0;
}
